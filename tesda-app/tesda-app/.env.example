# Copy this file to .env (same folder) and fill in your real MySQL
# password. .env is git-ignored, so your password never gets committed
# or pushed to GitHub — only this .env.example template is.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=tesda_stvet
PORT=3000
